"use strict";
const router = require('express').Router();

// Communications routes — email, calendar, contacts, messaging
// Agents add new routes here. server.js auto-loads this file.

module.exports = router;
