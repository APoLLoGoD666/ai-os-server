"use strict";
const router = require('express').Router();

// Intelligence routes — research, news, web search, knowledge
// Agents add new routes here. server.js auto-loads this file.

module.exports = router;
