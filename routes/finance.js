"use strict";
const router = require('express').Router();

// Finance routes — banking, transactions, budgets, investments
// Agents add new routes here. server.js auto-loads this file.

module.exports = router;
