"use strict";
const router = require('express').Router();

// Operations routes — files, system, automation, integrations
// Agents add new routes here. server.js auto-loads this file.

module.exports = router;
