services:
  - type: web
    name: ai-os-server
    env: node
    buildCommand: pip install graphifyy; npm install --legacy-peer-deps && npx playwright install chromium --with-deps
    startCommand: node --max-old-space-size=460 server.js
