# graphify
- **graphify** (`.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, invoke the Skill tool with `skill: "graphify"` before doing anything else.

# ui-ux
- **ui-ux** (`.claude/skills/ui-ux/SKILL.md`) - UI/UX design intelligence: 10-priority framework, accessibility rules, Apex design tokens, style patterns, pre-delivery checklist. Trigger: `/ui-ux`
When the user types `/ui-ux`, invoke the Skill tool with `skill: "ui-ux"` before doing anything else.
